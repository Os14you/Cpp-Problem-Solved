# Is prefix?

- `bool is_prefix(string main, string prefix, int start_pos = 0)`

#### Example
```
    is_prefix(“abcdefgh”, “abcd”)
    >> true
    is_prefix(“abcdefgh”, “”) 
    >> true
    is_prefix(“abcdefgh”, “abd”)
    >> false
```

